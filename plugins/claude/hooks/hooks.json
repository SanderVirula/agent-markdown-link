{
  "description": "Load explicitly configured curated Markdown at session start.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/runtime/session-start.mjs"
            ],
            "timeout": 10
          }
        ]
      }
    ]
  }
}
